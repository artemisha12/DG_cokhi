import '@/app/globals.css';
import './admin.css';

export const metadata = {
  title: 'Admin — Đức Giáp Cơ Khí',
  robots: 'noindex, nofollow',
};

export default function AdminLayout({ children }) {
  return children;
}
